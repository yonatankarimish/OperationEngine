package com.SixSense.data.logging;

//These names should match the <Logger name="..." > property in log4j2.xml configuration file
public enum Loggers {
    SessionLogger, CommandLogger, TerminalLogger, FileLogger
}
